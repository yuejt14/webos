import type { FC } from "react";
import styles from "./HelloWorld.module.scss";

const HelloWorld: FC = () => (
  <h1 className={styles.container}>Hellosaa,World!@! new deploy</h1>
);
export default HelloWorld;
